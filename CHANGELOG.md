# Changelog

## [Unreleased] — 发版前审查修复

### 本地化（简体中文 / English）

- **界面全面本地化**，跟随系统语言。此前 230 条中文字面量硬编码、`NSLocalizedString` 用量为 0——
  README 与徽章是英文，装上却是纯中文界面。
- 机制上踩过两个**静默失败**的坑，都已验证并写进代码注释：
  - `.xcstrings`（String Catalog）在 SwiftPM 下**不工作**：没有编译规则，JSON 被原样拷进包，
    查表返回 key 本身，且不报错。改用 `.lproj/Localizable.strings`。
  - `Bundle.module` 是陷阱：SwiftUI 的 `Text("字面量")` 默认查 **`Bundle.main`**，
    而 SwiftPM 把 target 资源放进 `Anchor_<Target>.bundle`。表放在 `Sources/` 之外、
    由打包脚本拷进 `Contents/Resources/`，两个 target 才能共用同一张表。
- 文案按位置说明符（`%1$@` / `%1$lld`）参数化，语序可随语言调整；
  含序数、复数的句子（「今天第 N 次摸鱼」）整句重写为与数量无关的表达，不需要 `.stringsdict`。
  「5 分钟」之类硬编码数字改为参数，改策略不必再改文案。
- 新增 `scripts/build-localizations.py`：合并分片、校验语言间 key 对齐与格式说明符一致性。
  **这一步是必需的**——查表失败会静默回落成 key，漏一条翻译在运行时只表现为界面上出现
  `settings.general.xxx`。已接入 CI，并有 7 个单测覆盖回落行为与生成物同步。
- Info.plist 补 `CFBundleDevelopmentRegion` 与 `CFBundleLocalizations`；
  在此之前系统「语言与地区 → 应用程序」里无法单独给 Anchor 选语言。

### 首启引导与权限

- **新增四步首启引导**（菜单栏 → 入门指南 可随时重看）。此前装完没有任何欢迎流程，
  而「绿区 / 灰区 / 红区 / 漂移 / 合法摸鱼」这套词汇从未向用户解释过，三个手势也无处可学。
  引导第一页同时教颜色与形状两套线索，并明确告知逃生通道（长按 3 秒）。每一步都可跳过。
- **权限不再静默失效**。全局快捷键与滚动锁都需要辅助功能权限，此前没授权就"静默降级"：
  按下毫无反应、开关打开却无效、没有任何状态或授权入口。现在设置里显示真实状态
  （已授权 / 被拒绝 / 尚未设置）并提供授权与直达系统设置的按钮；权限**只在用户主动点击时**
  才请求——绝不在启动数秒后突然弹出"某应用想接收你的按键"。

### 无障碍

- 全局快捷键改按**物理键位**（keyCode）匹配。此前用 `charactersIgnoringModifiers`，
  那是经键盘布局映射后的字符：AZERTY / Dvorak / 非拉丁布局下，文档写着 ⌃⌥⌘A
  按下去却什么都不会发生。
- 滚动锁**放过投给 Anchor 自己的滚动事件**。此前全会话吞掉滚轮，包括设置窗口和复盘窗口
  （两者都是 ScrollView）——一旦触发，用户就滚不到那个用来关掉它的开关。
- 摩擦雾尊重系统「降低透明度」（顶格不透明度压到 0.45）与「减少动态效果」（不做淡入）。
  此前 `.behindWindow` vibrancy 在降低透明度设置下会退化成不透明填充，
  最不能承受遮挡的用户反而得到最实的一层雾。
- 修正 spec / roadmap 里写错的快捷键修饰键（文档写 ⌥⌘，实现是 ⌃⌥⌘）。
- 修正 README「启动后屏幕顶部就有呼吸绿点」的错误描述——绿点只在进入绿区 app 后才出现。


一轮完整的 pre-launch 审查（安全 / 隐私 / 正确性 / 产品 / 无障碍）产出的修复。
**安全项建议尽快发版**：0.1.1 及更早版本存在可被任意网页利用的本机接口。

### 安全

- **本机 WebSocket 握手校验来源**（影响 ≤0.1.1）。Chrome 把 `127.0.0.1` 视作可信来源，
  因此此前**任何** `https://` 网页都能连上 `ws://127.0.0.1:17604`，进而伪造 `tab_active`
  事件（把自己伪装成绿区以消掉 friction、污染漂移库），并收到含用户绿区 URL 的广播指令。
  现在握手阶段即拒绝一切可证明来自网页的来源（`http(s)://` 与 sandbox iframe 的 `null`）。
  采用"拒可证伪者、放行其余"的策略而非白名单，以免浏览器改变 Origin 行为时打死核心功能。
- 单条消息上限 64KB（此前无上限：单帧 400MB 可把进程推入 swap）。
- 连接数封顶 8，且**满时淘汰最无身份的连接**而非拒绝新连接——否则本机进程占满坑位
  就能把真扩展锁在门外。同名浏览器只保留最新连接，指令投递不再依赖字典随机序。
- 移除未使用的 `com.apple.security.automation.apple-events` entitlement 与对应用途说明
  （代码中没有任何 Apple Events 调用）。
- 扩展只上报 / 只接受 `http(s)` 地址：`data:` / `blob:` / `file:` / `view-source:`
  不再被上报或落库，`navigate` 指令也不再能被用作跳转注入原语。

### 隐私

- **网址落盘前脱敏**：`?` 与 `#` 之后一律丢弃（搜索词、分享令牌、会话参数、重置密码链接
  不再进磁盘）。迁移会**就地清理历史记录**并 `VACUUM`。
- 数据库与 WAL/SHM 收紧为 `0600`、目录 `0700`（此前 `0644`，同机其它用户可直接读走）。
- 新增 设置 → **隐私与数据**：导出全部数据（JSON）、一键清除历史（保留场景规则）、
  保留时长（默认 90 天，可选 30 / 365 / 永久）。留存清理在启动时执行。
- 新增 [PRIVACY.md](PRIVACY.md)；README 补上唯一的出网行为——Sparkle 更新检查会向
  `raw.githubusercontent.com` 暴露 IP 与版本（默认关闭）。

### 修复

- **「再来 5 分钟」此前完全失效**：模态框在 `state = next` **之前**弹出，用户点确认后
  重入产生的 `.slacking` 会被外层帧用 `.offline` 覆盖，于是立刻被拉回，且已扣掉一次日额度。
- **第二天起每日复盘恒为 0 分**：菜单栏 app 长驻不换 session，而复盘按
  `started_at >= 今天零点` 查询，从第二天起恒为空。新增本地午夜切分（夏令时安全）。
- **睡眠时间被算作专注**：合盖一晚会给绿区灌进 ~8 小时、最长连续专注被刷成整夜，
  漂移记录出现"某站点 666 分钟"。现在入睡收口区间、唤醒重新判定并按需跨天。
- **正常退出会丢掉收尾写入**：`ended_at` 永远是 NULL（下次启动把正常退出误判为崩溃恢复），
  最后一条漂移记录直接丢失。退出路径加写队列栅栏。
- **「漂移倒计时」滑杆此前是装饰品**：`driftThreshold` 被存下来但从不参与判定，
  friction 曲线恒为 30/60/180s。现在整条曲线按阈值等比缩放。
- **暂停不再能被手势绕过**：通配手势 case 排在 `(.paused, _)` 之前，导致暂停期间
  按 ⌃⌥⌘B 会直接跳进摸鱼态，绕过恢复流程并丢弃暂停理由。
- **迁移改为原子提交** + `busy_timeout`：此前"建完表但没写 user_version"时被杀，
  下次启动会因重复建表而抛错，导致**永久静默不落盘**（规则存不住、复盘永远无数据）。
- 留存清理修正外键顺序：崩溃恢复写回的 `ended_at` 可能早于其子漂移，按时间删会留下孤儿行。
- 浏览器内遮罩会**残留**：此前只在升到 heavy 时下发、从不在降级时撤销。
- 设置里的扩展连接状态**会说谎**：该键被持久化，上次连着退出后下次启动仍显示"已连接"。
- 「检查更新」在无更新源时不再把 `scripts/appcast-template.xml` 这种仓库路径甩给用户。
- 不再把 `SUEnableAutomaticChecks` 钉成 `false`——那会连带压掉 Sparkle 自己的首次征询，
  结果是绝大多数用户永远收不到更新（含安全修复）。改为由 Sparkle 正常询问一次。

### 产品与无障碍

- **首次启动不再无故糊屏**。新用户装完通常停在 Finder / 浏览器（灰区），此前 60s 起
  屏幕渐雾、180s 到 0.8 并显示红色「立即拉回」，且点岛只会提示"暂无可拉回的目标"、
  下一秒又被糊回来。现在遵循一条原则：**没有锚点就没有橡皮筋**——当前不存在可拉回目标时
  不施加任何 friction。
- **「随便看看」不再永久重度模糊**。它绿区规则为空、注释写着"只做统计"，实际却让所有 app
  落灰区并持续加重 friction——最像逃生口的选项反而最难受。空绿区场景（含用户新建的空场景）
  现在是 `isObserveOnly`：只记录、不干预。
- 灰区深度漂移不再被渲染成红区：此前 3 分钟后切换成红色「立即拉回」胶囊，
  VoiceOver 也会念"红区"（对灰区是错误信息），且命令式口吻正是产品哲学拒绝的狱卒语气。
- **非颜色状态线索**：产品全部语义此前只靠一颗 8pt 圆点的色相表达（绿/琥珀/红/蓝/灰），
  红绿色盲用户无法区分"专注中"与"红区"。现在叠加形状符号做冗余编码。
- **尊重系统「减少动态效果」**：常驻呼吸点的无限动画此前无条件安装、没有任何路径能关掉。
- 修复灵动岛漂移态在浅色模式下**白字白底不可见**（该形态是唯一写死 `.white` 却没有背景的）。
- Focus Lock 菜单四项此前首次打开时全部可见且点了没反应（含一个"解除锁定"）。
- 删除场景加确认对话框（此前直接删除、不可撤销）；「关于」页补上项目主页 / 反馈 /
  隐私说明 / 许可证链接；设置窗口改为可随内容长大，避免长语言下裁切。
- 复盘日期与摸鱼日计数改用 locale 无关的稳定 key（`DayKey`）。此前用裸 `DateFormatter`
  生成 `yyyy-MM-dd`，会继承用户日历——泰历下是 `2569-…`、回历下是阿拉伯数字，
  而该字符串同时是 `daily_recaps` 的**主键**，改一次语言就会把历史复盘重新 key。

### 工程

- `Package.resolved` 纳入版本控制（此前被 gitignore，配合 `from:` 开区间意味着
  签名公证的发布产物**不可复现**，上游任意新版本会被自动带进来）。
- CI 加 `-warnings-as-errors`（守住"零警告"的既有声明）、打包冒烟与启动冒烟
  （打包脚本此前只在打 tag 时才跑）。
- 新增 22 个回归测试，覆盖上述每一项行为改动，含一个端到端测试证明网页来源
  连不上、且带对照组防止假阳性。测试总数 153 → 175。
- 清掉 `.gitignore` 里 UDS 时代的残留条目（`~/.anchor/`、`anchor.sock`）。

## [0.1.1] - 2026-06-12

刘海屏体验修复（用户实测反馈）：

- 展开面板宽度与硬件刘海严格相等（顶角半径 0，不再外扩 30pt）
- 修复拉回后"双绿点 + 卡顿"：移除 hover 保持行为（hide 不再被悬停阻塞循环重试），
  展开壳内不再渲染休眠点，compact 完全交给零覆盖侧点
- 拉回目标 app 名超长截断，避免文本撑宽面板
- App / dmg 图标改为直接光栅化品牌 SVG（与 README 视觉完全一致）

## [0.1.0] - 2026-06-12

首个功能完备版本。

### 核心
- 三态状态机（绿/灰/红）+ 纯函数 reducer + 渐进 friction 曲线
- 场景（Preset）引擎：app 精确匹配 + URL `*` 通配（tab 级粒度）；图标化编辑器、
  运行中应用点选、菜单栏一键收编（互斥 + 立即重判）
- Focus Lock「只看这个」：页面 / 站点 / app 三档临时锁定（⌥⌘L）
- Chrome 扩展（MV3）+ 本机 WebSocket daemon（127.0.0.1:17604），断线指数重连
- SQLite 持久化（WAL + 迁移框架）、60s checkpoint、crash 恢复
- 每日 22:00 叙事化复盘（Deep Score、24h 时间线、本周热力图、罪人榜、漂移链）
- 周日 21:00 周建议引擎（红区 / 场景调整 / 节律三类规则，一键 apply、一周可撤销）
- 合法摸鱼软/硬上限（日计数 3 次软）；暂停需 ≥10 字理由

### 灵动岛
- 真刘海：绿区零覆盖侧点（贴硬件刘海，下沿绝对对齐）+ 展开岛形面板
- 无刘海：菜单栏嵌入胶囊（宽度=内容，空闲点击穿透不挡系统图标）
- 三手势（单击拉回 / 长按摸鱼 / 上划暂停）+ 全局热键 ⌥⌘A/B/L/P
- FrictionFog 全屏渐进模糊（每屏独立，200ms ease-out，可一键关）

### 工程
- 149 个单元测试（含 WebSocket 真连接集成测试）；Swift 6 严格并发零警告
- 打包脚本（签名/公证/dmg）、CI、release workflow、Sparkle 脚手架
